# Local logs

`npm run test:smoke` writes workflow logs/screenshots under `logs/smoke`. Generated output is ignored. Copy only deliberate immutable evidence into the matching `releases/vX.Y.Z/evidence/` directory.

# Releases and immutable evidence

Each `vX.Y.Z` directory may contain installable/source archives, `SHA256SUMS.txt`, a machine-readable release receipt, notes, and selected test evidence. Once published, treat these files as immutable; corrections require a new version unless only remote release prose changes.

# v0.14.1 Live Pulse Hotfix

Release staging marker for the production pipeline. The canonical installable/source ZIPs, SHA-256 sums, and release receipt are produced reproducibly by `npm run package` and attached to the GitHub release.

Primary fixes: truthful live multi-tab Chat Pulse, background completion notifications, already-open-tab DOM probing after upgrades, and toggleable editable Branch & Continue with Enter-to-submit / Shift+Enter newline behavior.

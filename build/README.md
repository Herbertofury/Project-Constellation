# Generated builds

`npm run build:dev` or `npm run build` recreates `build/unpacked` and `build/build-info.json`. Generated contents are ignored. Load `build/unpacked` in Chrome for development/acceptance.

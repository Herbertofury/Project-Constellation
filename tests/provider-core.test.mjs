import assert from 'node:assert/strict';import fs from 'node:fs';import vm from 'node:vm';
const source=fs.readFileSync(new URL('../extension/src/provider-core.js',import.meta.url),'utf8');const context={globalThis:{},module:{exports:{}},URL};context.globalThis=context;vm.createContext(context);vm.runInContext(source,context);const p=context.ProjectConstellationProviders;
const manifest=JSON.parse(fs.readFileSync(new URL('../extension/manifest.json',import.meta.url),'utf8'));for(const provider of p.PROVIDERS)for(const host of provider.hosts)assert.ok(manifest.host_permissions.some(pattern=>pattern.includes(`://${host}/`)),`Missing host permission for ${provider.id}: ${host}`);
assert.equal(p.detectProvider('https://chatgpt.com/c/abc').id,'chatgpt');assert.equal(p.chatIdFromUrl('https://chatgpt.com/c/abc'),'chatgpt:abc');assert.equal(p.detectProvider('https://grok.com/').id,'grok');assert.equal(p.detectProvider('https://chat.deepseek.com/').id,'deepseek');assert.equal(p.detectProvider('https://gemini.google.com/app/123').id,'gemini');assert.equal(p.isLikelyChatUrl('https://claude.ai/chat/123','claude'),true);assert.equal(p.classifyExternalUrl('https://drive.google.com/file/d/abc/view').provider,'google-drive');assert.equal(p.classifyExternalUrl('https://github.com/user/repo/releases').provider,'github');assert.equal(p.byId.chatgpt.catalog.manualFullCapture,true);assert.equal(p.byId.deepseek.catalog.manualFullCapture,true);
const added=[['https://www.meta.ai/','metaai'],['https://chat.qwen.ai/c/abc','qwen'],['https://www.kimi.com/chat/abc','kimi'],['https://character.ai/chat/abc','characterai'],['https://huggingface.co/chat/conversation/abc','huggingchat'],['https://you.com/chat/abc','you'],['https://pi.ai/talk/abc','pi'],['https://duck.ai/','duckai']];for(const [url,id] of added)assert.equal(p.detectProvider(url)?.id,id);assert.equal(p.PROVIDERS.length,17);assert.equal(p.byId.duckai.catalog.backgroundHtml,false);assert.equal(p.byId.metaai.connection.oauthHistory,false);console.log('provider-core.test.mjs: PASS');

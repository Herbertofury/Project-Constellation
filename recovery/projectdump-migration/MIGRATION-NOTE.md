# ProjectDump migration note

This tree is the preserved Project Constellation-specific subtree from `Herbertofury/ProjectDump` at the migration audit point. It retains historic automation, research, publication, recovery, release, and wiki-maintainer state without altering or importing unrelated ProjectDump content.

The active runtime source is `../../extension/` at repository root. Filtered ProjectDump ancestry is joined into the standalone repository history with an ours-strategy migration merge; this snapshot preserves the exact state that was intentionally not placed at the new root.
